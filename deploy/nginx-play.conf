# aaPanel / Nginx example for clean player URLs.
# Add this location block inside the server { } block for the player site.
location ^~ /play/ {
    try_files $uri $uri/ /index.php?$query_string;
}

# Keep normal PHP processing as configured by aaPanel.
